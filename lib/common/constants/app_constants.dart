class AppConstants {
  const AppConstants._();

  static const String appName = 'e-Tithe';
  static const String versionLabel = 'V.1.0.0.5';
  static const Duration splashDuration = Duration(milliseconds: 2500);
  static const Duration loginDuration = Duration(milliseconds: 900);
}
